function scoreChecker(score) {
  let result = 80;

  // TODO
  if (result >= 90) {
    console.log('Selamat! Anda mendapatkan nilai A');
  } else {
    console.log('Maaf, Anda belum mendapatkan nilai A');
  }

  // Jangan hapus kode ini
  return result;
}

/**
 * Jangan hapus kode di bawah ini
 */
module.exports = scoreChecker;
